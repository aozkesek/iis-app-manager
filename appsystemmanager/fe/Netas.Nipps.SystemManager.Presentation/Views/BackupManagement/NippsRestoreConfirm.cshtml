@model Netas.Nipps.SystemManager.Presentation.Models.Nipps

@section featured {
    <section class="featured">
        <div class="content-wrapper">
            <hgroup class="title">
                <h1>@Resources.Global.RestoreTitle</h1>
            </hgroup>
        </div>
    </section>
}

@using (Html.BeginForm("NippsRestore", "BackupManagement", FormMethod.Post, new { @id="restoreForm" }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)
    
    @CommonComponentHelper.NippsHostSitePoolApplication(
        @Url.Action("", "BackupManagement"), 
        Netas.Nipps.SystemManager.Presentation.Base.MaintenanceOperation.Restore, 
        Model)

    <fieldset>
        <ol>
            <li>
                @Html.Label(Resources.Global.Restore)
                @Html.TextBox("Restore", (string)ViewBag.RestoreItem, new { @style="width:600px;", @readonly="true" })
            </li>
            <li>
                <button class="cancel" onclick="$('#restoreForm').Nipps_SubmitTo('@Url.Action("NippsServiceList", "DeployManagement")', 'Get'); ">@Resources.Global.Cancel</button>
                <input type="submit" value="@Resources.Global.Restore" />
            </li>
        </ol>
    </fieldset>
    

}
